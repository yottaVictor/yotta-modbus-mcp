# Yotta Modbus MCP Server — 測試報告

## 版本 v4.4.0　｜　2026-03-10

**工具數**：11 個 MCP Tools

---

## 測試總結

v4.4.0 新增三項工具（`ping_device`、`list_channels`、`rename_channel`）並強化設備管理的防護機制（`name` 為唯一識別主鍵、相同 IP 不允許重複 Port、`update_device` 新增衝突檢查）。全部 11 個工具及所有防禦機制均測試通過。

---

## 測試結果明細

### 一、資訊查詢 ✅

1. **`list_devices`**：正確列出全部 5 台設備（辦公室、一樓電箱、倉庫、測試機、書房）。
2. **`lookup_device_spec`**：
   - A-1869：DO×8（baseAddress: 16）
   - A-1812：DI×2、AI×4、AO×2

### 二、連線診斷 ✅

3. **`ping_device`**：
   - 單台（辦公室）：正常，177ms。
   - `all`：辦公室、一樓電箱、測試機、書房正常；倉庫離線（已知狀況）。

### 三、新增設備與防禦機制 ✅

4. **`add_device` 正常新增**：成功新增「測試設備」（A1869，192.168.0.100:502，DO×8）。
5. **Name 重複攔截**：嘗試新增名為「辦公室」的設備，正確回傳 `❌ 設備名稱 "辦公室" 已存在`。
6. **IP+Port 衝突攔截**：嘗試新增 192.168.10.10:502，正確回傳 `❌ IP 192.168.10.10:502 已被設備 "辦公室" 使用`。

### 四、設備維護 ✅

7. **`update_device` 正常更新**：成功將「測試設備」改名為「測試設備改名」並更改 IP。
8. **`update_device` 改名衝突攔截**：嘗試改名為「辦公室」，正確回傳 `❌ 設備名稱 "辦公室" 已被其他設備使用`。
9. **`update_device` IP+Port 衝突攔截**：嘗試改為 192.168.10.10:502，正確回傳 `❌ IP 192.168.10.10:502 已被設備 "辦公室" 使用`。
10. **`list_channels`**：正確列出辦公室 DO 標籤（大燈、電動門等）及測試機 AI 標籤（溫度、溼度）。
11. **`rename_channel`**：將測試機 DI0 命名為「測試前門」→ 驗證顯示正確 → 清除命名。

### 五、I/O 讀取 ✅

12. **`read_io`**：
    - 辦公室 all / DO：正確回傳 DO0~DO7 狀態，標籤（大燈、電動門）顯示正確。
    - 測試機 all：DI、AI、AO 全部正確回傳，自訂標籤（溫度、溼度、馬達）顯示正確。
    - 一樓電箱 AI：正確回傳 AI0~AI7。

### 六、I/O 控制 ✅

13. **`write_do`**：
    - 單個：DO6 開啟，回傳 `✅ 辦公室 DO6 已開啟`。
    - 批量：DO0、DO1 開啟，DO7 關閉，標籤正確顯示於回傳（大燈→ON、DO1→ON、大燈→OFF）。
14. **`write_ao`**：
    - AO0 設為 1000，回傳 `✅ AO0 已設為 1000`。
    - AO1（馬達）設為 0，標籤「馬達」正確顯示。

### 七、組合操作 ✅

- 讀取辦公室 DO 狀態，找出 OFF 的通道（DO4、DO5、DO7），一次批量寫入全開。
- 最終所有通道均為 ON，驗證通過。

### 八、錯誤處理 ✅

| 情境 | 預期錯誤 | 實際結果 |
|------|---------|---------|
| 讀取不存在設備 XXXXX | 回傳有效名稱列表 | ✅ 正確，列出 6 台有效設備 |
| 開啟辦公室 DO99 | DO99 超出範圍 | ✅ 正確，提示最大 DO7 |
| 查詢型號 Z-9999 | 找不到規格資料 | ✅ 正確 |

### 九、通道編號正確性驗證 ✅

- 指定開啟 DO6，讀取後確認 DO6 為 ON，其他通道不受影響。

### 十、環境復原 ✅

- `remove_device`：成功移除「測試設備改名」。
- `list_devices` 確認設備清單回到原始 5 台。

---

## 版本差異對照

| 項目 | v4.2.0 | v4.4.0 |
|------|--------|--------|
| 工具數 | 8 | 11（+ping_device, list_channels, rename_channel）|
| 設備唯一識別 | `id` | `name` |
| add_device 防護 | 無 | Name 重複 + IP+Port 衝突攔截 |
| update_device 防護 | 無 | Name 重複 + IP+Port 衝突攔截 |

**核准狀況**：全部 11 個工具及防禦機制測試通過，系統穩定運作。
